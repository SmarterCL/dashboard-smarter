/**
 * URLs canónicas del sistema de auth/onboarding.
 *
 * Usadas por componentes de landing y auth para construir
 * botones de signup/login sin hardcodear paths.
 */
export const AUTH_URL = "/auth"
export const LOGIN_URL = "/auth?mode=login"
export const SIGNUP_URL = "/auth?mode=signup"
